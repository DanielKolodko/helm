apiVersion: v1
kind: Service
metadata:
  name: {{ include "cat-gifs.fullname" . }}
  labels:
    {{- include "cat-gifs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cat-gifs.selectorLabels" . | nindent 4 }}
